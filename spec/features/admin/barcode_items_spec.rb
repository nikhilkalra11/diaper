RSpec.feature "Barcode Items Admin" do
  context "while signed in as a super admin" do
    before do
      sign_in @super_admin
    end

    scenario "creating a new global barcode" do
    end

    scenario "editing an existing global barcode" do
    end

    scenario "deleting a global barcode" do
    end

    scenario "viewing a barcode shows details about it" do
    end
  end
end